apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "api-docs.fullname" . }}-test-connection"
  labels:
    {{- include "api-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "api-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
