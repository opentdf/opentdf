apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-docs.fullname" . }}-test-connection"
  labels:
    {{- include "go-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
