apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "eth-pdp.fullname" . }}-test-connection"
  labels:
    {{- include "eth-pdp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "eth-pdp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
