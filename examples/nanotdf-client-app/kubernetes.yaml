apiVersion: apps/v1
kind: Deployment
metadata:
  name: nanotdf-client-app
spec:
  selector:
    matchLabels:
      app: nanotdf-client-app
  template:
    metadata:
      labels:
        app: nanotdf-client-app
    spec:
      containers:
        - name: nanotdf-client-app
          env:
          - name: KAS_URL
            value: "http://opentdf-key-access:65432"
          - name: OIDC_ENDPOINT
            value: "http://opentdf-keycloak"
          image: opentdf/nanotdf-client-app-image
          ports:
            - containerPort: 65432
---
apiVersion: v1
kind: Service
metadata:
  name: nanotdf-client-app
  labels:
    service: nanotdf-client-app
spec:
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: nanotdf-client-app
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /$1
  name: nanotdf-client-app-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - backend:
              service:
                name: nanotdf-client-app
                port:
                  number: 3000
            path: /nanotdf-client-app/(.*)
            pathType: Prefix